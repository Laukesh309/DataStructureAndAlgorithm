package HeapChapter;

import java.util.PriorityQueue;

public class connectRoap {

    public static int minimumCast(int arr[]) {
        PriorityQueue<Integer> pq = new PriorityQueue<>();
        System.out.println("");
        // for (int i = 0; i < arr.length; i++) {
        // pq.add(arr[i]);
        // }
        // int totalCast = 0;
        // while (!pq.size() == 1) {
        // int counte = 0;
        // int firstRop = 0;
        // int secoondRop = 0;

        // }

        return 1;

    }

    public static void main(String[] args) {
        int arr[] = { 1, 2, 3, 4, 5 };
        minimumCast(arr);

    }

}
